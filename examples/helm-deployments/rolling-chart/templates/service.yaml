apiVersion: v1
kind: Service
metadata:
  name: {{ include "rolling-app.fullname" . }}
  labels:
    {{- include "rolling-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rolling-app.selectorLabels" . | nindent 4 }}