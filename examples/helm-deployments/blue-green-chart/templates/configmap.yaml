apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blue-green-app.fullname" . }}-config
  labels:
    {{- include "blue-green-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

